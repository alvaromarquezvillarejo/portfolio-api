# Portfolio API - FastAPI

API desarrollada en **Python** usando **FastAPI** y **SQLAlchemy** para demostrar un portfolio de programación.

## Tecnologías

- Python 3.14  
- FastAPI  
- SQLAlchemy  
- Pydantic  
- JWT (python-jose)  
- Passlib (bcrypt)  
- Python-multipart  
- SQLite (o tu motor de DB)

## Instalación

1. Clonar el repositorio:

```bash
git clone <URL_DE_TU_REPO>
cd API

# Portfolio API - FastAPI

Este proyecto es un **portfolio de API REST** desarrollado con **FastAPI** y **SQLAlchemy**, incluyendo:

- Registro y login de usuarios con JWT
- Roles `user` y `admin`
- Endpoints protegidos según rol
- CRUD completo de usuarios
- Documentación automática con Swagger UI

## Instalación

```bash
git clone https://github.com/tu_usuario/portfolio-api.git
cd portfolio-api
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
